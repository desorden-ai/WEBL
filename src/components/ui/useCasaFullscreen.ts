import {useEffect,useState} from 'react';
export function useCasaFullscreen(){const[isFullscreen,setIsFullscreen]=useState(false);useEffect(()=>{const handle=()=>setIsFullscreen(!!document.fullscreenElement);document.addEventListener('fullscreenchange',handle);return()=>document.removeEventListener('fullscreenchange',handle)},[]);const toggleFullscreen=async()=>{try{if(!document.fullscreenElement){if(document.documentElement.requestFullscreen)await document.documentElement.requestFullscreen()}else if(document.exitFullscreen)await document.exitFullscreen()}catch(err){console.warn('Fullscreen request failed or restricted:',err)}};return{isFullscreen,toggleFullscreen}}
