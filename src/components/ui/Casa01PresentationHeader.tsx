import React from 'react';
import {Sun,Sunset,Moon,Maximize,Minimize} from 'lucide-react';
import {TimeOfDay} from '../../types';
export const Casa01PresentationHeader:React.FC<{timeOfDay:TimeOfDay;setTimeOfDay:(t:TimeOfDay)=>void;isFullscreen:boolean;toggleFullscreen:()=>void}>=({timeOfDay,setTimeOfDay,isFullscreen,toggleFullscreen})=><header className="absolute top-4 left-4 right-4 flex items-center justify-between pointer-events-none z-10"><div className="pointer-events-auto flex items-center space-x-3 bg-black/60 backdrop-blur-md px-3.5 py-2 rounded-xl border border-white/10 shadow-lg"><div className="w-2.5 h-2.5 rounded-full bg-emerald-400 animate-pulse"/><div><h1 className="text-xs font-semibold tracking-wider text-white uppercase">CASA 01 — SOL</h1><p className="text-[10px] text-zinc-400 font-medium">Refined Architecture & Landscape</p></div></div><div className="pointer-events-auto flex items-center bg-black/60 backdrop-blur-md p-1 rounded-xl border border-white/10 shadow-lg space-x-1"><button onClick={()=>setTimeOfDay('DAY')} className={`flex items-center space-x-1.5 px-2.5 py-1.5 rounded-lg text-xs font-medium transition-all ${timeOfDay==='DAY'?'bg-amber-500/20 text-amber-300 border border-amber-500/30':'text-zinc-400 hover:text-white hover:bg-white/5'}`} title="Day Lighting"><Sun className="w-3.5 h-3.5"/><span className="hidden sm:inline">DAY</span></button><button onClick={()=>setTimeOfDay('SUNSET')} className={`flex items-center space-x-1.5 px-2.5 py-1.5 rounded-lg text-xs font-medium transition-all ${timeOfDay==='SUNSET'?'bg-orange-500/20 text-orange-300 border border-orange-500/30':'text-zinc-400 hover:text-white hover:bg-white/5'}`} title="Sunset Lighting"><Sunset className="w-3.5 h-3.5"/><span className="hidden sm:inline">SUNSET</span></button><button onClick={()=>setTimeOfDay('NIGHT')} className={`flex items-center space-x-1.5 px-2.5 py-1.5 rounded-lg text-xs font-medium transition-all ${timeOfDay==='NIGHT'?'bg-indigo-500/20 text-indigo-300 border border-indigo-500/30':'text-zinc-400 hover:text-white hover:bg-white/5'}`} title="Night Lighting"><Moon className="w-3.5 h-3.5"/><span className="hidden sm:inline">NIGHT</span></button><div className="w-[1px] h-4 bg-white/15 my-auto mx-0.5"/><button onClick={toggleFullscreen} className={`flex items-center justify-center p-1.5 rounded-lg text-xs font-medium transition-all ${isFullscreen?'bg-white/20 text-white border border-white/30':'text-zinc-400 hover:text-white hover:bg-white/5'}`} title={isFullscreen?'Exit Fullscreen':'Enter Fullscreen'} aria-label={isFullscreen?'Exit Fullscreen':'Enter Fullscreen'}>{isFullscreen?<Minimize className="w-3.5 h-3.5"/>:<Maximize className="w-3.5 h-3.5"/>}</button></div></header>;
