import React from 'react';
import {Info} from 'lucide-react';
import {CameraPreset} from '../../types';
export const Casa01PresetStrip:React.FC<{preset:CameraPreset;setPreset:(p:CameraPreset)=>void;showInfo:boolean;setShowInfo:(s:boolean)=>void}>=({preset,setPreset,showInfo,setShowInfo})=><div className="pointer-events-auto flex items-center bg-black/75 backdrop-blur-md p-1.5 rounded-2xl border border-white/10 shadow-xl space-x-1 overflow-x-auto max-w-full">{(['OVERVIEW','FACADE','TERRACE','GROUND'] as CameraPreset[]).map(p=><button key={p} onClick={()=>setPreset(p)} className={`px-3 py-1.5 rounded-xl text-[11px] font-medium tracking-wide uppercase transition-all whitespace-nowrap ${preset===p?'bg-white/20 text-white border border-white/20 font-semibold':'text-zinc-400 hover:text-white hover:bg-white/5'}`}>{p}</button>)}<button onClick={()=>setShowInfo(!showInfo)} className={`p-2 rounded-xl transition-all ${showInfo?'bg-white/20 text-white':'text-zinc-400 hover:text-white hover:bg-white/5'}`} title="Project Information"><Info className="w-4 h-4"/></button></div>;
