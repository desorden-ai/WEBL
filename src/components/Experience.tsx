import { CinematicScroll } from './CinematicScroll';

export default function Experience() {
  return (
    <main className="w-full min-h-screen bg-black text-neutral-100 font-sans">
      <CinematicScroll />
    </main>
  );
}
