export type FarTree={pos:[number,number,number];scale:[number,number,number];rot:number;variant:number};
export function makeDistantBands(){const bandA:FarTree[]=[],bandB:FarTree[]=[],bandC:FarTree[]=[];let seed=98765;const rnd=()=>{seed=(seed*9301+49297)%233280;return seed/233280};for(let i=0;i<32;i++){const a=(i/32)*Math.PI*2+(rnd()-.5)*.2,r=30+rnd()*12,x=Math.cos(a)*r,z=Math.sin(a)*r;if(z>4&&z<32&&Math.abs(x)<15)continue;bandA.push({pos:[x,0,z],scale:[.88+rnd()*.18,.92+rnd()*.16,.88+rnd()*.18],rot:rnd()*Math.PI*2,variant:Math.floor(rnd()*4)})}for(let i=0;i<36;i++){const a=(i/36)*Math.PI*2+(rnd()-.5)*.2,r=42+rnd()*13,x=Math.cos(a)*r,z=Math.sin(a)*r;if(z>10&&z<40&&Math.abs(x)<16)continue;bandB.push({pos:[x,0,z],scale:[.85+rnd()*.18,.88+rnd()*.16,.85+rnd()*.18],rot:rnd()*Math.PI*2,variant:Math.floor(rnd()*4)})}for(let i=0;i<40;i++){const a=(i/40)*Math.PI*2+(rnd()-.5)*.2,r=55+rnd()*15,x=Math.cos(a)*r,z=Math.sin(a)*r;if(z>18&&z<50&&Math.abs(x)<18)continue;bandC.push({pos:[x,0,z],scale:[.82+rnd()*.18,.84+rnd()*.16,.82+rnd()*.18],rot:rnd()*Math.PI*2,variant:Math.floor(rnd()*4)})}return{bandA,bandB,bandC}}
